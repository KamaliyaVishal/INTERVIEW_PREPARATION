# Java Collections Framework - Complete Hierarchy

This document provides a comprehensive visual representation of the Java Collections Framework hierarchy.

---

## 📊 High-Level Overview

```
                                    ┌─────────────────┐
                                    │    Iterable     │  (java.lang)
                                    │   <interface>   │
                                    └────────┬────────┘
                                             │
                                             ▼
                                    ┌─────────────────┐
                                    │   Collection    │  (java.util)
                                    │   <interface>   │
                                    └────────┬────────┘
                                             │
              ┌──────────────────────────────┼──────────────────────────────┐
              │                              │                              │
              ▼                              ▼                              ▼
     ┌─────────────────┐           ┌─────────────────┐           ┌─────────────────┐
     │      List       │           │       Set       │           │      Queue      │
     │   <interface>   │           │   <interface>   │           │   <interface>   │
     └─────────────────┘           └─────────────────┘           └─────────────────┘


                                    ┌─────────────────┐
                                    │       Map       │  (Separate hierarchy)
                                    │   <interface>   │
                                    └─────────────────┘
```

---

## 📋 1. LIST Interface Hierarchy

```
                                    ┌─────────────────┐
                                    │   Collection    │
                                    │   <interface>   │
                                    └────────┬────────┘
                                             │ extends
                                             ▼
                                    ┌─────────────────┐
                                    │      List       │
                                    │   <interface>   │
                                    └────────┬────────┘
                                             │
           ┌─────────────────────────────────┼─────────────────────────────────┐
           │                                 │                                 │
           │ implements                      │ implements                      │ implements
           ▼                                 ▼                                 ▼
  ┌─────────────────┐              ┌─────────────────┐              ┌─────────────────┐
  │   ArrayList     │              │   LinkedList    │              │     Vector      │
  │     <class>     │              │     <class>     │              │     <class>     │
  │                 │              │                 │              │  (Legacy/Sync)  │
  │ • Dynamic array │              │ • Doubly linked │              └────────┬────────┘
  │ • Fast random   │              │ • Fast insert/  │                       │ extends
  │   access O(1)   │              │   delete O(1)   │                       ▼
  │ • Slow insert/  │              │ • Slow random   │              ┌─────────────────┐
  │   delete O(n)   │              │   access O(n)   │              │     Stack       │
  └─────────────────┘              │ • Implements    │              │     <class>     │
                                   │   Deque too     │              │  (Legacy/LIFO)  │
                                   └─────────────────┘              └─────────────────┘

  ┌─────────────────┐
  │ CopyOnWriteArray│
  │      List       │
  │     <class>     │
  │ (Thread-safe)   │
  └─────────────────┘
```

### List Implementations Summary

| Class | Underlying Structure | Thread-Safe | Performance |
|-------|---------------------|-------------|-------------|
| `ArrayList` | Dynamic Array | ❌ No | Fast read O(1), Slow insert O(n) |
| `LinkedList` | Doubly Linked List | ❌ No | Fast insert O(1), Slow read O(n) |
| `Vector` | Dynamic Array | ✅ Yes (synchronized) | Slower due to sync |
| `Stack` | Dynamic Array | ✅ Yes | LIFO operations |
| `CopyOnWriteArrayList` | Array | ✅ Yes | Fast read, Slow write |

---

## 🔵 2. SET Interface Hierarchy

```
                                    ┌─────────────────┐
                                    │   Collection    │
                                    │   <interface>   │
                                    └────────┬────────┘
                                             │ extends
                                             ▼
                                    ┌─────────────────┐
                                    │       Set       │
                                    │   <interface>   │
                                    │ • No duplicates │
                                    └────────┬────────┘
                                             │
              ┌──────────────────────────────┼──────────────────────────────┐
              │                              │                              │
              │ extends                      │ implements                   │ extends
              ▼                              ▼                              ▼
     ┌─────────────────┐           ┌─────────────────┐           ┌─────────────────┐
     │    SortedSet    │           │    HashSet      │           │  NavigableSet   │
     │   <interface>   │           │     <class>     │           │   <interface>   │
     │ • Sorted order  │           │                 │           │ • Navigation    │
     └────────┬────────┘           │ • Hash table    │           │   methods       │
              │                    │ • O(1) ops      │           └────────┬────────┘
              │ extends            │ • No order      │                    │
              ▼                    └────────┬────────┘                    │ implements
     ┌─────────────────┐                    │ extends                     ▼
     │  NavigableSet   │                    ▼                    ┌─────────────────┐
     │   <interface>   │           ┌─────────────────┐           │    TreeSet      │
     └────────┬────────┘           │ LinkedHashSet   │           │     <class>     │
              │                    │     <class>     │           │                 │
              │ implements         │                 │           │ • Red-Black Tree│
              ▼                    │ • Insertion     │           │ • Sorted O(log n)│
     ┌─────────────────┐           │   order         │           │ • NavigableSet  │
     │    TreeSet      │◄──────────│ • Hash + Linked │           └─────────────────┘
     │     <class>     │           └─────────────────┘
     └─────────────────┘

     ┌─────────────────┐           ┌─────────────────┐
     │    EnumSet      │           │ CopyOnWriteArray│
     │     <class>     │           │       Set       │
     │ • For enums     │           │     <class>     │
     │ • Very fast     │           │ (Thread-safe)   │
     └─────────────────┘           └─────────────────┘

     ┌─────────────────┐
     │ConcurrentSkipList│
     │       Set       │
     │     <class>     │
     │ (Thread-safe)   │
     └─────────────────┘
```

### Set Implementations Summary

| Class | Underlying Structure | Ordering | Thread-Safe |
|-------|---------------------|----------|-------------|
| `HashSet` | HashMap | ❌ No order | ❌ No |
| `LinkedHashSet` | HashMap + LinkedList | ✅ Insertion order | ❌ No |
| `TreeSet` | Red-Black Tree | ✅ Sorted (natural/comparator) | ❌ No |
| `EnumSet` | Bit Vector | ✅ Enum order | ❌ No |
| `CopyOnWriteArraySet` | Array | ✅ Insertion order | ✅ Yes |
| `ConcurrentSkipListSet` | Skip List | ✅ Sorted | ✅ Yes |

---

## 📤 3. QUEUE Interface Hierarchy

```
                                    ┌─────────────────┐
                                    │   Collection    │
                                    │   <interface>   │
                                    └────────┬────────┘
                                             │ extends
                                             ▼
                                    ┌─────────────────┐
                                    │      Queue      │
                                    │   <interface>   │
                                    │ • FIFO (usually)│
                                    └────────┬────────┘
                                             │
              ┌──────────────────────────────┼──────────────────────────────┐
              │                              │                              │
              │ extends                      │ implements                   │ implements
              ▼                              ▼                              ▼
     ┌─────────────────┐           ┌─────────────────┐           ┌─────────────────┐
     │      Deque      │           │  PriorityQueue  │           │   LinkedList    │
     │   <interface>   │           │     <class>     │           │     <class>     │
     │ • Double-ended  │           │                 │           │ (also List)     │
     └────────┬────────┘           │ • Heap-based    │           └─────────────────┘
              │                    │ • Priority order│
              │                    │ • O(log n)      │
              │                    └─────────────────┘
              │
    ┌─────────┴─────────────────────────────────────────┐
    │                       │                           │
    │ implements            │ implements                │ implements
    ▼                       ▼                           ▼
┌─────────────────┐ ┌─────────────────┐       ┌─────────────────┐
│  ArrayDeque     │ │   LinkedList    │       │ ConcurrentLinked│
│    <class>      │ │    <class>      │       │      Deque      │
│                 │ │                 │       │    <class>      │
│ • Resizable     │ │ • Doubly linked │       │ (Thread-safe)   │
│ • Faster than   │ └─────────────────┘       └─────────────────┘
│   LinkedList    │
│ • No null       │
└─────────────────┘


                    ┌─────────────────────────────────────────────┐
                    │           BLOCKING QUEUES                    │
                    │              (java.util.concurrent)          │
                    └─────────────────────────────────────────────┘

                                    ┌─────────────────┐
                                    │  BlockingQueue  │
                                    │   <interface>   │
                                    └────────┬────────┘
                                             │
         ┌───────────────┬───────────────────┼───────────────────┬───────────────┐
         │               │                   │                   │               │
         ▼               ▼                   ▼                   ▼               ▼
┌─────────────┐ ┌─────────────┐    ┌─────────────┐    ┌─────────────┐ ┌─────────────┐
│ArrayBlocking│ │LinkedBlocking│    │ PriorityBl- │    │Synchronous- │ │   Delay     │
│   Queue     │ │    Queue    │    │ ockingQueue │    │    Queue    │ │   Queue     │
│   <class>   │ │   <class>   │    │   <class>   │    │   <class>   │ │   <class>   │
│ • Bounded   │ │ • Optionally│    │ • Priority  │    │ • Zero      │ │ • Delayed   │
│ • Array     │ │   bounded   │    │   ordering  │    │   capacity  │ │   elements  │
└─────────────┘ └─────────────┘    └─────────────┘    └─────────────┘ └─────────────┘
```

### Queue Implementations Summary

| Class | Type | Bounded | Thread-Safe | Ordering |
|-------|------|---------|-------------|----------|
| `LinkedList` | Queue/Deque | ❌ No | ❌ No | FIFO |
| `PriorityQueue` | Queue | ❌ No | ❌ No | Priority (Heap) |
| `ArrayDeque` | Deque | ❌ No | ❌ No | FIFO/LIFO |
| `ArrayBlockingQueue` | BlockingQueue | ✅ Yes | ✅ Yes | FIFO |
| `LinkedBlockingQueue` | BlockingQueue | Optional | ✅ Yes | FIFO |
| `PriorityBlockingQueue` | BlockingQueue | ❌ No | ✅ Yes | Priority |
| `SynchronousQueue` | BlockingQueue | Zero | ✅ Yes | Direct handoff |
| `DelayQueue` | BlockingQueue | ❌ No | ✅ Yes | Delay-based |

---

## 🗺️ 4. MAP Interface Hierarchy

```
                                    ┌─────────────────┐
                                    │       Map       │
                                    │   <interface>   │
                                    │ • Key-Value     │
                                    │ • Unique keys   │
                                    └────────┬────────┘
                                             │
      ┌──────────────────────────────────────┼──────────────────────────────────────┐
      │                                      │                                      │
      │ extends                              │ implements                           │ extends
      ▼                                      ▼                                      ▼
┌─────────────────┐                 ┌─────────────────┐                 ┌─────────────────┐
│    SortedMap    │                 │     HashMap     │                 │  ConcurrentMap  │
│   <interface>   │                 │     <class>     │                 │   <interface>   │
│ • Sorted keys   │                 │                 │                 │ • Thread-safe   │
└────────┬────────┘                 │ • Hash table    │                 │   operations    │
         │                          │ • O(1) ops      │                 └────────┬────────┘
         │ extends                  │ • Null key/value│                          │
         ▼                          │   allowed       │                          │ implements
┌─────────────────┐                 └────────┬────────┘                          ▼
│  NavigableMap   │                          │ extends                  ┌─────────────────┐
│   <interface>   │                          ▼                          │ConcurrentHashMap│
│ • Navigation    │                 ┌─────────────────┐                 │     <class>     │
│   methods       │                 │  LinkedHashMap  │                 │                 │
└────────┬────────┘                 │     <class>     │                 │ • Segmented     │
         │                          │                 │                 │   locking       │
         │ implements               │ • Insertion/    │                 │ • High          │
         ▼                          │   Access order  │                 │   concurrency   │
┌─────────────────┐                 │ • LRU cache     │                 │ • No null       │
│    TreeMap      │                 └─────────────────┘                 └─────────────────┘
│     <class>     │
│                 │                 ┌─────────────────┐                 ┌─────────────────┐
│ • Red-Black Tree│                 │    Hashtable    │                 │ConcurrentSkipList│
│ • Sorted O(log n)│                │     <class>     │                 │       Map       │
│ • NavigableMap  │                 │   (Legacy)      │                 │     <class>     │
└─────────────────┘                 │ • Synchronized  │                 │ • Sorted        │
                                    │ • No null       │                 │ • Thread-safe   │
                                    └─────────────────┘                 └─────────────────┘

┌─────────────────┐                 ┌─────────────────┐                 ┌─────────────────┐
│    WeakHashMap  │                 │   IdentityHash  │                 │     EnumMap     │
│     <class>     │                 │       Map       │                 │     <class>     │
│                 │                 │     <class>     │                 │                 │
│ • Weak keys     │                 │ • Reference     │                 │ • For enum keys │
│ • GC eligible   │                 │   equality (==) │                 │ • Very fast     │
└─────────────────┘                 └─────────────────┘                 └─────────────────┘
```

### Map Implementations Summary

| Class | Underlying Structure | Ordering | Thread-Safe | Null Keys/Values |
|-------|---------------------|----------|-------------|------------------|
| `HashMap` | Hash Table | ❌ No | ❌ No | ✅ Yes / ✅ Yes |
| `LinkedHashMap` | Hash Table + LinkedList | ✅ Insertion/Access | ❌ No | ✅ Yes / ✅ Yes |
| `TreeMap` | Red-Black Tree | ✅ Sorted | ❌ No | ❌ No / ✅ Yes |
| `Hashtable` | Hash Table | ❌ No | ✅ Yes | ❌ No / ❌ No |
| `ConcurrentHashMap` | Segmented Hash Table | ❌ No | ✅ Yes | ❌ No / ❌ No |
| `ConcurrentSkipListMap` | Skip List | ✅ Sorted | ✅ Yes | ❌ No / ❌ No |
| `WeakHashMap` | Hash Table | ❌ No | ❌ No | ✅ Yes / ✅ Yes |
| `IdentityHashMap` | Hash Table | ❌ No | ❌ No | ✅ Yes / ✅ Yes |
| `EnumMap` | Array | ✅ Enum order | ❌ No | ❌ No / ✅ Yes |

---

## 🔄 5. Complete Hierarchy Diagram

```
java.lang.Iterable
        │
        └── java.util.Collection
                │
                ├── java.util.List
                │       ├── ArrayList
                │       ├── LinkedList ─────────────────┐
                │       ├── Vector                      │
                │       │       └── Stack               │
                │       └── CopyOnWriteArrayList        │
                │                                       │
                ├── java.util.Set                       │
                │       ├── HashSet                     │
                │       │       └── LinkedHashSet       │
                │       ├── SortedSet                   │
                │       │       └── NavigableSet        │
                │       │               └── TreeSet     │
                │       ├── EnumSet                     │
                │       ├── CopyOnWriteArraySet         │
                │       └── ConcurrentSkipListSet       │
                │                                       │
                └── java.util.Queue ◄───────────────────┘
                        │
                        ├── PriorityQueue
                        ├── Deque
                        │       ├── ArrayDeque
                        │       ├── LinkedList
                        │       └── ConcurrentLinkedDeque
                        │
                        └── BlockingQueue
                                ├── ArrayBlockingQueue
                                ├── LinkedBlockingQueue
                                ├── PriorityBlockingQueue
                                ├── SynchronousQueue
                                ├── DelayQueue
                                └── BlockingDeque
                                        └── LinkedBlockingDeque


java.util.Map (Separate Hierarchy)
        │
        ├── HashMap
        │       └── LinkedHashMap
        │
        ├── SortedMap
        │       └── NavigableMap
        │               └── TreeMap
        │
        ├── Hashtable (Legacy)
        │       └── Properties
        │
        ├── WeakHashMap
        ├── IdentityHashMap
        ├── EnumMap
        │
        └── ConcurrentMap
                ├── ConcurrentHashMap
                └── ConcurrentNavigableMap
                        └── ConcurrentSkipListMap
```

---

## 🛠️ 6. Utility Classes

```
┌─────────────────────────────────────────────────────────────────────┐
│                      java.util.Collections                          │
│                      (Utility Class)                                │
├─────────────────────────────────────────────────────────────────────┤
│  • sort()              - Sort a list                                │
│  • binarySearch()      - Binary search in sorted list               │
│  • reverse()           - Reverse a list                             │
│  • shuffle()           - Randomly shuffle elements                  │
│  • swap()              - Swap two elements                          │
│  • fill()              - Replace all elements with specified value  │
│  • copy()              - Copy elements from one list to another     │
│  • min() / max()       - Find minimum/maximum element               │
│  • frequency()         - Count occurrences                          │
│  • disjoint()          - Check if collections have no common elements│
│  • synchronizedXxx()   - Create synchronized wrapper                │
│  • unmodifiableXxx()   - Create immutable wrapper                   │
│  • checkedXxx()        - Create type-safe wrapper                   │
│  • emptyXxx()          - Return empty immutable collection          │
│  • singletonXxx()      - Return single-element collection           │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                        java.util.Arrays                             │
│                      (Utility Class)                                │
├─────────────────────────────────────────────────────────────────────┤
│  • asList()            - Convert array to List                      │
│  • sort()              - Sort an array                              │
│  • parallelSort()      - Parallel sorting                           │
│  • binarySearch()      - Binary search in sorted array              │
│  • equals()            - Compare two arrays                         │
│  • fill()              - Fill array with value                      │
│  • copyOf()            - Copy array with new length                 │
│  • copyOfRange()       - Copy portion of array                      │
│  • toString()          - String representation                      │
│  • deepToString()      - Deep string representation                 │
│  • stream()            - Create stream from array                   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📊 7. When to Use Which Collection

```
┌─────────────────────────────────────────────────────────────────────┐
│                    DECISION FLOWCHART                               │
└─────────────────────────────────────────────────────────────────────┘

                            START
                              │
                              ▼
                    ┌─────────────────┐
                    │ Need Key-Value? │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              │ YES                         │ NO
              ▼                             ▼
     ┌─────────────────┐           ┌─────────────────┐
     │   Use MAP       │           │ Allow Duplicates?│
     └────────┬────────┘           └────────┬────────┘
              │                             │
              │                  ┌──────────┴──────────┐
              │                  │ YES                 │ NO
              │                  ▼                     ▼
              │         ┌─────────────────┐   ┌─────────────────┐
              │         │    Use LIST     │   │    Use SET      │
              │         └────────┬────────┘   └────────┬────────┘
              │                  │                     │
              ▼                  ▼                     ▼
     ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
     │ Need Sorting?   │ │ Need Fast       │ │ Need Sorting?   │
     └────────┬────────┘ │ Random Access?  │ └────────┬────────┘
              │          └────────┬────────┘          │
     ┌────────┴────────┐          │         ┌────────┴────────┐
     │YES         │NO  │ ┌────────┴────────┐│YES         │NO  │
     ▼            ▼    │ │YES         │NO  │▼            ▼    │
  TreeMap    HashMap   │ ▼            ▼    │TreeSet   HashSet │
                       │ArrayList  LinkedList               │
                       │                                    │
                       └──────────────────────────────────────┘
```

---

## 🔒 8. Thread-Safe Collections Summary

```
┌────────────────────────────────────────────────────────────────────────────┐
│                         THREAD-SAFE OPTIONS                                 │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  LEGACY (Synchronized - Lower Performance)                                 │
│  ├── Vector                                                                │
│  ├── Stack                                                                 │
│  └── Hashtable                                                             │
│                                                                            │
│  SYNCHRONIZED WRAPPERS (Collections.synchronizedXxx)                       │
│  ├── Collections.synchronizedList(new ArrayList<>())                       │
│  ├── Collections.synchronizedSet(new HashSet<>())                          │
│  └── Collections.synchronizedMap(new HashMap<>())                          │
│                                                                            │
│  CONCURRENT COLLECTIONS (java.util.concurrent - Best Performance)          │
│  ├── CopyOnWriteArrayList     - Thread-safe List                           │
│  ├── CopyOnWriteArraySet      - Thread-safe Set                            │
│  ├── ConcurrentHashMap        - Thread-safe HashMap                        │
│  ├── ConcurrentSkipListMap    - Thread-safe Sorted Map                     │
│  ├── ConcurrentSkipListSet    - Thread-safe Sorted Set                     │
│  ├── ConcurrentLinkedQueue    - Thread-safe Queue                          │
│  ├── ConcurrentLinkedDeque    - Thread-safe Deque                          │
│  └── BlockingQueue variants   - Producer-Consumer patterns                 │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 📝 Quick Reference Card

| Need | Use | Why |
|------|-----|-----|
| Fast random access by index | `ArrayList` | O(1) get by index |
| Frequent insert/delete | `LinkedList` | O(1) insert/delete |
| No duplicates, fast lookup | `HashSet` | O(1) contains |
| No duplicates, sorted | `TreeSet` | O(log n) sorted |
| No duplicates, insertion order | `LinkedHashSet` | O(1) + ordered |
| Key-value, fast lookup | `HashMap` | O(1) get/put |
| Key-value, sorted keys | `TreeMap` | O(log n) sorted |
| Key-value, insertion order | `LinkedHashMap` | O(1) + ordered |
| FIFO queue | `ArrayDeque` or `LinkedList` | O(1) enqueue/dequeue |
| Priority queue | `PriorityQueue` | O(log n) priority |
| Thread-safe list | `CopyOnWriteArrayList` | Read-heavy scenarios |
| Thread-safe map | `ConcurrentHashMap` | High concurrency |
| Thread-safe queue | `ConcurrentLinkedQueue` | Non-blocking |
| Producer-consumer | `BlockingQueue` | Thread coordination |

---

*Document generated: December 2024*
*Java Version: 8+ (includes java.util.concurrent)*

